# 一个月前端与开源冲刺方案

目标不是“学完所有前端知识”，而是在一个月内完成三件可验证的事：

- 独立完成并上线个人博客 MVP。
- 能判断 GitHub 仓库中常见文件的作用，并顺着代码找到一个功能。
- 至少提交 1 个符合规范的真实开源 PR，争取完成 2–3 次高质量尝试。

## 每天的学习节奏

每天控制在约 5–6 小时有效学习，剩余时间用于休息：

- 90 分钟：freeCodeCamp 新内容。
- 60 分钟：关闭提示，在 VS Code 重新实现。
- 120 分钟：开发自己的博客。
- 45 分钟：Git、GitHub 或阅读真实仓库。
- 30 分钟：写学习日志和提交 Git commit。

如果临时被打断，不补课、不赶进度，直接从下一个未完成单元继续。离开前在 `NEXT.md` 写三句话：做到哪里、目前问题、回来后的第一步。

## 第一周：HTML、Git 与博客骨架

学习 [Responsive Web Design v9](https://www.freecodecamp.org/learn/responsive-web-design-v9/) 中这些内容：

- Computer Basics。
- Basic HTML。
- Semantic HTML。
- HTML Forms and Tables。
- HTML and Accessibility。

同时学习 Git 最小命令集：

```powershell
git status
git add
git commit
git log
git branch
git switch
git pull
git push
git clone
```

第一天完成：

1. 从 [Node.js 官网](https://nodejs.org/en/download)安装当前 LTS 版；制定计划时官网提供的是 Node.js 24 LTS。
2. 配置 Git 姓名、邮箱和默认分支：

```powershell
git config --global user.name "你的 GitHub 用户名"
git config --global user.email "你的 GitHub noreply 邮箱"
git config --global init.defaultBranch main
```

3. 创建公开仓库 `learning-log`，每天用中文记录学习内容，保留英文技术术语。
4. 按 [GitHub Hello World](https://docs.github.com/en/get-started/using-github/hello-world) 在自己的仓库完成一次 `branch → commit → Pull Request → merge` 演练。
5. 创建第二个公开仓库 `my-blog`。

博客先建立这个结构：

```text
my-blog/
├── index.html
├── reviews.html
├── diary.html
├── todo.html
├── posts/
│   ├── first-book-review.html
│   ├── first-movie-review.html
│   └── first-diary.html
├── assets/
│   ├── css/styles.css
│   ├── js/main.js
│   ├── js/todo.js
│   └── images/
├── README.md
├── LICENSE
└── .gitignore
```

第一周验收：关闭 freeCodeCamp，独立写出一个包含页眉、导航、正文、图片、文章卡片和页脚的语义化 HTML 页面。

## 第二周：CSS 与响应式博客

freeCodeCamp 必学：

- Basic CSS、单位、颜色和层叠规则。
- Box Model。
- Flexbox 和 Grid。
- Typography。
- Forms。
- Positioning。
- Responsive Design。
- CSS Variables。
- CSS Accessibility。

动画、复杂设计理论和认证考试可以延后。

把博客做到：

- 手机、平板和桌面宽度均可正常阅读。
- 导航、文章卡片、分类页面具有统一设计。
- 有清楚的字体层级、间距和颜色变量。
- 键盘可以访问主要链接和表单。
- 图片具有 `alt`，表单具有真实的 `label`。
- 至少发布一篇书评、一篇影评和一篇公开日记。

不要在公开日记中放真实住址、电话、证件、账号、行程、密钥或其他敏感信息。

第二周验收：在 360px、768px 和桌面宽度下检查页面，没有横向滚动、文字溢出或无法点击的控件。

## 第三周：JavaScript 与 Todo

学习 [JavaScript v9](https://www.freecodecamp.org/learn/javascript-v9/) 中：

- Variables、Strings、Booleans、Numbers。
- Functions。
- Arrays、Objects、Loops。
- Higher-order Functions 和 Callbacks。
- DOM Manipulation 和 Events。
- Debugging。
- Form Validation。
- LocalStorage 和 CRUD。
- Asynchronous JavaScript。

正则、音视频事件、Map/Set、类、递归和函数式编程暂时延后。遇到不懂的概念，只把 [MDN Learn Web Development](https://developer.mozilla.org/en-US/docs/Learn_web_development) 当参考资料，不再同时开启第二套完整课程。

给博客加入：

- 深浅色切换并记住选择。
- 书评、影评和日记分类筛选。
- Todo 的新增、完成、删除和状态筛选。
- 使用 `localStorage` 在刷新后保留 Todo。
- 空输入校验与明确的错误提示。

第三周验收：

1. 关闭教程，从空的 `todo.js` 重新实现核心功能。
2. 刷新页面后任务仍存在。
3. 删除全部任务、输入空格、重复点击等情况不会报错。
4. 能用自己的话解释 DOM、事件、数组和 `localStorage` 如何串联起来。

## 第四周：上线、读仓库、提交 PR

GitHub Pages 可以免费发布公开仓库中的静态文件，入口文件使用根目录的 `index.html`。[官方发布说明](https://docs.github.com/en/pages/getting-started-with-github-pages/creating-a-github-pages-site)

上线前完成：

- README：项目介绍、功能、截图、目录结构、运行方法和后续计划。
- 检查所有页面链接和图片。
- 使用浏览器开发者工具清理控制台错误。
- 从一个全新目录重新 `git clone`，仅根据 README 成功运行项目。
- 给正式版本打一个 `v1.0.0` tag。

## GitHub 文件阅读顺序

不要从第一个源文件开始逐行硬啃。进入陌生仓库后按这个顺序：

| 文件或目录 | 先回答什么 |
|---|---|
| `README.md` | 项目解决什么问题，如何安装和运行？ |
| `CONTRIBUTING.md` | 怎样提 Issue、建分支、测试和提交 PR？ |
| `LICENSE` | 是否允许使用、修改和分发？ |
| `package.json` | 有哪些依赖和可运行的 `scripts`？ |
| `package-lock.json` 等 | 锁定了哪些确切依赖版本？通常不要手改 |
| `.gitignore` | 哪些本地文件不能提交？ |
| `.env.example` | 项目需要哪些环境变量？绝不能提交真实 `.env` 或密钥 |
| `src/`、`public/`、`tests/` | 源码、静态资源和测试分别在哪里？ |
| `.github/` | Issue/PR 模板和自动化检查是什么？ |
| 配置文件 | ESLint、Prettier、Vite、测试工具如何工作？ |

然后只追踪一个小功能：

```text
用户操作
→ HTML 元素
→ 事件监听器
→ 被调用的函数
→ 读取或修改的数据
→ 页面更新
→ 对应测试
```

每个陌生文件只问五件事：谁使用它、它引入什么、接收什么、输出什么、怎样验证它正确。

## 首个开源 PR 流程

优先寻找 `good first issue`、`help wanted`、`documentation` 标签。GitHub 官方同样建议新贡献者先从文档改进或小修复开始，并在动手前阅读项目规范。[开源贡献指南](https://docs.github.com/en/get-started/exploring-projects-on-github/contributing-to-open-source)

执行顺序：

1. 确认仓库仍然活跃，并阅读 README、CONTRIBUTING 和近期类似 PR。
2. 阅读 Issue 全部讨论，确认没有其他人正在处理。
3. 没有明确邀请贡献时，先留言说明你的理解和修改方案。
4. Fork、clone，并创建单独分支。
5. 只修改解决该问题所必需的内容。
6. 执行项目要求的测试、格式检查和构建。
7. 自己审查 diff，排除临时文件和无关格式变化。
8. 提交 PR，写清问题、解决方式、验证过程和相关 Issue。
9. 维护者要求修改时，继续在同一个 PR 中更新。

首月 PR 可以依次尝试：文档或失效链接 → HTML/CSS 小修复 → 示例或测试补充 → 简单 JavaScript 修复。被拒绝不等于失败，但每次必须记录原因并改进下一次提交。

## 最终验收清单

月底达到以下标准，就算完成第一阶段：

- 博客已经公开上线。
- 至少包含书评、影评、日记和可持久化 Todo。
- 能脱离教程修改 HTML、CSS 和 JavaScript。
- 能解释至少 10 种常见 GitHub 文件。
- 能独立完成 clone、branch、commit、push 和 PR。
- 已提交至少一个真实、合规、非练习性质的开源 PR。
- 学习日志记录了关键错误、解决过程和每周复盘。
- React、后端和数据库仍保持延后，没有挤占基础训练。
